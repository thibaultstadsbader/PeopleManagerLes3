@model IList<Person>
@{
    ViewData["Title"] = "Home Page";
    var firstPerson = Model.FirstOrDefault();
}

@if(firstPerson is not null)
{
    <partial name="PersonPartial" model="firstPerson"/>
}

@foreach (var person in Model)
{
    <partial name="PersonPartial" model="person"/>
}
